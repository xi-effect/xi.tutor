# Umami Events — Сводное описание

Полный реестр событий аналитики, сгруппированный по страницам приложения.

---

## Глобальные / Сессионные

| Событие                            | Способ вызова                                                    | Описание                                                        |
| ---------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------- |
| `trackUmamiSession` (signin)       | `window.umami.identify()` — программно после входа               | Идентификация пользователя при авторизации                      |
| `trackUmamiSession` (signup)       | `window.umami.identify()` — программно после регистрации         | Идентификация пользователя при регистрации                      |
| `trackUmamiSession` (session_init) | `window.umami.identify()` — программно при восстановлении сессии | Идентификация пользователя при инициализации сессии             |
| `role-change`                      | `window.umami.track()` — программно при смене роли               | Переключение роли (репетитор ↔ ученик), свойства: `from`, `to`  |
| `onboarding-complete`              | `window.umami.track()` — программно при завершении тура          | Завершение онбординг-тура, свойство: `layout` (tutor / student) |

---

## Навигация (все страницы)

**Модуль:** `modules.navigation`

| Событие                        | Способ вызова                                            | Описание                                                                                                                  |
| ------------------------------ | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `navigation-{titleKey}`        | `data-umami-event` — клик по пункту бокового меню        | Переход по навигации, доп. свойство `url`. Значения `titleKey`: `home`, `schedule`, `classrooms`, `materials`, `payments` |
| `navigation-mobile-{titleKey}` | `data-umami-event` — клик по пункту мобильного меню      | Переход по навигации, доп. свойство `url`                                                                                 |
| `navigation-mobile-logo`       | `data-umami-event` — клик по логотипу в нижней панели    | Переход на главную (мобильная)                                                                                            |
| `navigation-mobile-menu`       | `data-umami-event` — клик по кнопке меню в нижней панели | Открытие мобильного меню                                                                                                  |
| `header-user-menu-open`        | `data-umami-event` — клик по аватару в шапке             | Открытие пользовательского меню, доп. свойство `device` (mobile)                                                          |
| `header-profile-open`          | `data-umami-event` — клик «Профиль» в меню пользователя  | Открытие профиля, доп. свойство `device` (mobile)                                                                         |
| `header-logout`                | `data-umami-event` — клик «Выйти» в меню пользователя    | Выход из аккаунта, доп. свойство `device` (mobile)                                                                        |
| `header-pwa-install`           | `data-umami-event` — клик «Установить приложение»        | Установка PWA, доп. свойство `device` (mobile)                                                                            |

### Модалка поддержки

| Событие                   | Способ вызова                                  | Описание                     |
| ------------------------- | ---------------------------------------------- | ---------------------------- |
| `support-telegram`        | `data-umami-event` — клик по контакту Telegram | Переход в Telegram-поддержку |
| `support-vk`              | `data-umami-event` — клик по контакту VK       | Переход в VK-поддержку       |
| `support-social-telegram` | `data-umami-event` — клик по иконке Telegram   | Переход в Telegram (соцсети) |
| `support-social-vk`       | `data-umami-event` — клик по иконке VK         | Переход в VK (соцсети)       |

---

## Профиль

**Модуль:** `modules.profile`

| Событие          | Способ вызова                     | Описание                              |
| ---------------- | --------------------------------- | ------------------------------------- |
| `profile-logout` | `data-umami-event` — клик «Выйти» | Выход из аккаунта на странице профиля |

---

## `/signin` — Вход

**Пакет:** `pages.signin`

| Событие                     | Способ вызова                                            | Описание                         |
| --------------------------- | -------------------------------------------------------- | -------------------------------- |
| `auth-signin-button`        | `data-umami-event` — клик по кнопке «Войти»              | Отправка формы авторизации       |
| `auth-forgot-password-link` | `data-umami-event` — клик по ссылке «Забыли пароль?»     | Переход на восстановление пароля |
| `auth-signup-link`          | `data-umami-event` — клик по ссылке «Зарегистрироваться» | Переход на регистрацию           |

---

## `/signup` — Регистрация

**Пакет:** `pages.signup`

| Событие               | Способ вызова                                                | Описание                                                               |
| --------------------- | ------------------------------------------------------------ | ---------------------------------------------------------------------- |
| `auth-signup-button`  | `data-umami-event` — клик по кнопке «Зарегистрироваться»     | Отправка формы регистрации                                             |
| `auth-signin-link`    | `data-umami-event` — клик по ссылке «Войти»                  | Переход на страницу входа                                              |
| `outbound-link-click` | `data-umami-event` — клик по ссылке на соглашение / политику | Переход на внешний документ, свойства: `url`, `type` (terms / privacy) |

---

## `/reset-password` — Восстановление пароля

**Пакет:** `pages.reset-password`

| Событие                      | Способ вызова                                      | Описание                             |
| ---------------------------- | -------------------------------------------------- | ------------------------------------ |
| `auth-reset-password-send`   | `data-umami-event` — клик «Отправить»              | Отправка запроса на сброс пароля     |
| `auth-reset-password-resend` | `data-umami-event` — клик «Отправить повторно»     | Повторная отправка ссылки для сброса |
| `auth-reset-password-signin` | `data-umami-event` — клик «Войти» (после отправки) | Переход на страницу входа            |
| `auth-new-password-save`     | `data-umami-event` — клик «Сохранить»              | Сохранение нового пароля             |
| `auth-signin-link`           | `data-umami-event` — клик по ссылке «Войти»        | Переход на страницу входа            |

---

## `/welcome/*` — Онбординг

**Пакет:** `pages.welcome`

| Событие                   | Способ вызова                                   | Описание                                            |
| ------------------------- | ----------------------------------------------- | --------------------------------------------------- |
| `welcome-next-button`     | `data-umami-event` — клик «Далее»               | Переход к следующему шагу онбординга                |
| `welcome-continue-button` | `data-umami-event` — клик «Продолжить»          | Продолжение онбординга                              |
| `welcome-back-button`     | `data-umami-event` — клик «Назад»               | Возврат к предыдущему шагу                          |
| `welcome-role-select`     | `data-umami-event` — выбор роли                 | Выбор роли на шаге онбординга, свойство: `role`     |
| `service-connect`         | `data-umami-event` — клик «Подключить»          | Подключение внешнего сервиса, свойство: `service`   |
| `service-external-link`   | `data-umami-event` — клик «Перейти в {service}» | Переход на сайт сервиса, свойства: `service`, `url` |

---

## `/confirm-email` — Подтверждение email

**Пакет:** `pages.email`

| Событие                       | Способ вызова                                  | Описание                                |
| ----------------------------- | ---------------------------------------------- | --------------------------------------- |
| `email-confirm-button-resend` | `data-umami-event` — клик «Отправить повторно» | Повторная отправка письма подтверждения |

---

## `/invite/$inviteId` — Приглашение

**Пакет:** `pages.invites`

| Событие          | Способ вызова                          | Описание                                 |
| ---------------- | -------------------------------------- | ---------------------------------------- |
| `invite-accept`  | `data-umami-event` — клик «Принять»    | Принятие приглашения, свойство: `kind`   |
| `invite-decline` | `data-umami-event` — клик «Отказаться» | Отклонение приглашения, свойство: `kind` |

---

## `/` — Главная

**Пакет:** `pages.main`

| Событие                           | Способ вызова                                                | Описание                                        |
| --------------------------------- | ------------------------------------------------------------ | ----------------------------------------------- |
| `main-create-group`               | `data-umami-event` — клик «Создать группу»                   | Открытие модалки создания группы                |
| `main-invite-student`             | `data-umami-event` — клик «Пригласить»                       | Открытие модалки приглашения ученика            |
| `main-first-lesson-guide-read`    | `data-umami-event` — клик «Читать» в баннере                 | Переход к гайду по первому занятию              |
| `main-first-lesson-guide-dismiss` | `data-umami-event` — клик «Скрыть» в баннере                 | Закрытие баннера первого занятия                |
| `classroom-open`                  | `data-umami-event` — клик по иконке открытия карточки класса | Переход в класс, свойство: `classroom-id`       |
| `classroom-start-lesson`          | `data-umami-event` — клик «Начать урок» (репетитор)          | Начало урока (ВКС), свойство: `classroom-id`    |
| `classroom-join-lesson`           | `data-umami-event` — клик «Присоединиться» (ученик)          | Присоединение к уроку, свойство: `classroom-id` |

### Панель «Расписание» (правая колонка)

**Модуль:** `modules.calendar`, **Пакет:** `pages.main`

| Событие                              | Способ вызова                                           | Описание                                        |
| ------------------------------------ | ------------------------------------------------------- | ----------------------------------------------- |
| `schedule-go-to-today`               | `data-umami-event` — клик «К сегодня»                   | Прокрутка карусели дат к сегодняшнему дню       |
| `add-lesson-button`                  | `data-umami-event` — клик «+» в заголовке               | Открытие модалки добавления занятия             |
| `schedule-date-select`               | `data-umami-event` — клик по дню в карусели             | Выбор даты, свойство: `is-today` (true / false) |
| `schedule-date-carousel-prev`        | `data-umami-event` — стрелка влево                      | Прокрутка карусели дат назад                    |
| `schedule-date-carousel-next`        | `data-umami-event` — стрелка вправо                     | Прокрутка карусели дат вперёд                   |
| `schedule-empty-add-lesson`          | `data-umami-event` — клик «Назначить занятие»           | Добавление занятия из пустого состояния дня     |
| `schedule-lesson-reschedule`         | `data-umami-event` — иконка «Перенести» на карточке     | Открытие модалки переноса занятия               |
| `schedule-lesson-edit`               | `data-umami-event` — иконка «Редактировать» на карточке | Открытие модалки редактирования занятия         |
| `schedule-lesson-cancel-init`        | `data-umami-event` — иконка «Отменить» на карточке      | Открытие модалки отмены занятия                 |
| `schedule-lesson-toggle-description` | `data-umami-event` — клик по карточке занятия           | Переключение отображения описания занятия       |

### Блок «Оплата» на главной

| Событие                  | Способ вызова                                   | Описание                            |
| ------------------------ | ----------------------------------------------- | ----------------------------------- |
| `create-invoice-button`  | `data-umami-event` — клик «+»                   | Открытие модалки создания счёта     |
| `payments-empty-help`    | `data-umami-event` — клик «Как работает оплата» | Переход в базу знаний по оплатам    |
| `payments-empty-invoice` | `data-umami-event` — клик «Создать счёт»        | Создание счёта из пустого состояния |
| `payments-empty-more`    | `data-umami-event` — клик «Показать все»        | Переход на страницу оплат           |

### Блок «Кабинеты» на главной

| Событие                   | Способ вызова                                      | Описание                         |
| ------------------------- | -------------------------------------------------- | -------------------------------- |
| `invite-student-button`   | `data-umami-event` — клик «Пригласить»             | Открытие модалки приглашения     |
| `classrooms-add-student`  | `data-umami-event` — пункт меню «Добавить ученика» | Добавление ученика               |
| `classrooms-add-group`    | `data-umami-event` — пункт меню «Создать группу»   | Создание группы                  |
| `classrooms-empty-invite` | `data-umami-event` — клик «Пригласить ученика»     | Приглашение из пустого состояния |

### Блок «Материалы» на главной

| Событие                     | Способ вызова                               | Описание                              |
| --------------------------- | ------------------------------------------- | ------------------------------------- |
| `materials-add-button`      | `data-umami-event` — клик «+»               | Открытие меню добавления материала    |
| `materials-add-board`       | `data-umami-event` — пункт «Доска»          | Создание доски                        |
| `materials-add-note`        | `data-umami-event` — пункт «Заметка»        | Создание заметки                      |
| `materials-empty-add-note`  | `data-umami-event` — клик «Создать заметку» | Создание заметки из пустого состояния |
| `materials-empty-add-board` | `data-umami-event` — клик «Создать доску»   | Создание доски из пустого состояния   |

---

## `/classrooms` — Список классов

**Пакет:** `pages.classrooms`

| Событие                      | Способ вызова                                                | Описание                            |
| ---------------------------- | ------------------------------------------------------------ | ----------------------------------- |
| `classrooms-create-group`    | `data-umami-event` — клик «Создать группу»                   | Создание группы                     |
| `classrooms-invite-student`  | `data-umami-event` — клик «Пригласить ученика»               | Открытие модалки приглашения        |
| `classrooms-page-empty-help` | `data-umami-event` — клик «Как работают кабинеты»            | Переход в базу знаний               |
| `material-card-open`         | `data-umami-event` — клик по карточке кабинета               | Открытие кабинета, свойство: `type` |
| `material-edit`              | `data-umami-event` — клик «Переименовать» в контекстном меню | Переименование кабинета             |

---

## `/classrooms/$classroomId` — Страница класса

**Пакет:** `pages.classroom`

| Событие                        | Способ вызова                                      | Описание                              |
| ------------------------------ | -------------------------------------------------- | ------------------------------------- |
| `classroom-back-to-classrooms` | `data-umami-event` — клик «Назад»                  | Возврат к списку кабинетов            |
| `classroom-add-student`        | `data-umami-event` — клик «Добавить ученика»       | Открытие модалки добавления ученика   |
| `classroom-invite-to-group`    | `data-umami-event` — клик «Пригласить в группу»    | Открытие модалки приглашения в группу |
| `classroom-add-lesson`         | `data-umami-event` — клик «Добавить занятие»       | Открытие модалки добавления занятия   |
| `classroom-create-invoice`     | `data-umami-event` — клик «Создать счёт на оплату» | Открытие модалки создания счёта       |
| `classroom-delete`             | `data-umami-event` — клик «Удалить кабинет»        | Удаление кабинета                     |

### Вкладка «Расписание» кабинета

**Модуль:** `modules.calendar` (внутри `pages.classroom`)

Используются те же события, что на странице `/schedule` (канбан, карточки занятий, модалки), кроме кнопки «Добавить занятие» в шапке — она открывается через `classroom-add-lesson`.

### Модалка приглашений (features.invites)

| Событие            | Способ вызова                                   | Описание                                              |
| ------------------ | ----------------------------------------------- | ----------------------------------------------------- |
| `invite-create`    | `data-umami-event` — клик «Создать приглашение» | Создание нового приглашения                           |
| `invite-copy-link` | `data-umami-event` — клик по иконке копирования | Копирование ссылки приглашения, свойство: `invite-id` |
| `invite-delete`    | `data-umami-event` — клик по иконке удаления    | Удаление приглашения, свойство: `invite-id`           |

### Модалка приглашения в группу (features.group.invite)

| Событие                         | Способ вызова                                      | Описание                                |
| ------------------------------- | -------------------------------------------------- | --------------------------------------- |
| `group-invite-copy-link-input`  | `data-umami-event` — клик по полю ввода со ссылкой | Копирование ссылки из поля              |
| `group-invite-menu-open`        | `data-umami-event` — клик по кнопке меню           | Открытие меню действий с приглашением   |
| `group-invite-reset`            | `data-umami-event` — клик «Сбросить»               | Сброс (пересоздание) ссылки приглашения |
| `group-invite-copy-link-button` | `data-umami-event` — клик «Копировать ссылку»      | Копирование ссылки приглашения          |

### Модалка создания группы (features.group.add)

| Событие               | Способ вызова                        | Описание               |
| --------------------- | ------------------------------------ | ---------------------- |
| `group-create`        | `data-umami-event` — клик «Создать»  | Создание группы        |
| `group-create-cancel` | `data-umami-event` — клик «Отменить» | Отмена создания группы |

### Модалка состава группы (features.group.manage)

| Событие                 | Способ вызова                              | Описание                                                       |
| ----------------------- | ------------------------------------------ | -------------------------------------------------------------- |
| `group-student-toggle`  | `data-umami-event` — переключатель ученика | Включение/исключение ученика из группы, свойство: `student-id` |
| `group-students-save`   | `data-umami-event` — клик «Сохранить»      | Сохранение состава группы                                      |
| `group-students-cancel` | `data-umami-event` — клик «Отменить»       | Отмена редактирования состава                                  |

### Модалка списка учеников (features.students.list)

| Событие                  | Способ вызова                        | Описание                                           |
| ------------------------ | ------------------------------------ | -------------------------------------------------- |
| `student-delete-init`    | `data-umami-event` — иконка удаления | Инициация удаления ученика, свойство: `student-id` |
| `student-delete-confirm` | `data-umami-event` — клик «Удалить»  | Подтверждение удаления, свойство: `student-id`     |
| `student-delete-cancel`  | `data-umami-event` — клик «Отменить» | Отмена удаления                                    |

---

## `/payments` — Оплаты

**Пакет:** `pages.payments`

| Событие                    | Способ вызова                                   | Описание                                 |
| -------------------------- | ----------------------------------------------- | ---------------------------------------- |
| `payments-page-empty-help` | `data-umami-event` — клик «Как работает оплата» | Переход в базу знаний (пустое состояние) |

### Создание счёта (features.invoice)

| Событие          | Способ вызова                                 | Описание                      |
| ---------------- | --------------------------------------------- | ----------------------------- |
| `invoice-create` | `data-umami-event` — клик «Создать» в модалке | Отправка формы создания счёта |

### Таблица счетов (features.table)

| Событие          | Способ вызова                                                 | Описание                               |
| ---------------- | ------------------------------------------------------------- | -------------------------------------- |
| `invoice-delete` | `data-umami-event` — клик по иконке удаления в строке таблицы | Удаление счёта, свойство: `invoice-id` |

### Подтверждение оплаты (features.payment.approve)

| Событие                    | Способ вызова                                                                      | Описание                                                                                    |
| -------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| `payment-approve-sender`   | `data-umami-event` — клик по кнопке подтверждения (статус «ждёт оплаты»)           | Открытие модалки подтверждения оплаты, свойство: `role`                                     |
| `payment-approve-receiver` | `data-umami-event` — клик по кнопке подтверждения (статус «ожидает подтверждения») | Прямое подтверждение получения средств репетитором (без модалки)                            |
| `payment-sender-confirm`   | `data-umami-event` — клик «Подтвердить» в модалке (AdvanceForm)                    | Подтверждение оплаты (ученик) или одностороннее подтверждение (репетитор), свойство: `role` |
| `payment-receiver-confirm` | `data-umami-event` — клик «Подтвердить» в модалке (ApproveForm)                    | Подтверждение получения средств репетитором в модалке                                       |

---

## `/materials` — Материалы

**Пакет:** `pages.materials`

| Событие                     | Способ вызова                                       | Описание                                 |
| --------------------------- | --------------------------------------------------- | ---------------------------------------- |
| `materials-create-material` | `data-umami-event` — клик по кнопке «+» (мобильная) | Создание нового материала                |
| `materials-page-empty-help` | `data-umami-event` — клик «Как работают материалы»  | Переход в базу знаний (пустое состояние) |

### Карточка материала (features.materials.card)

| Событие                           | Способ вызова                                | Описание                               |
| --------------------------------- | -------------------------------------------- | -------------------------------------- |
| `material-card-open`              | `data-umami-event` — клик по карточке        | Открытие материала, свойство: `type`   |
| `material-actions-menu-open`      | `data-umami-event` — клик по меню действий   | Открытие контекстного меню             |
| `material-change-access-open`     | `data-umami-event` — пункт «Изменить доступ» | Открытие подменю доступа               |
| `material-access-mode-change`     | `data-umami-event` — выбор режима доступа    | Смена режима, свойства: `mode`, `from` |
| `material-duplicate-to-classroom` | `data-umami-event` — пункт «Дублировать»     | Открытие модалки дублирования          |
| `material-edit`                   | `data-umami-event` — пункт «Переименовать»   | Открытие модалки переименования        |
| `material-delete`                 | `data-umami-event` — пункт «Удалить»         | Удаление материала (общий список)      |
| `material-delete-from-classroom`  | `data-umami-event` — пункт «Удалить»         | Удаление материала из кабинета         |

### Редактирование названия (features.materials.edit)

| Событие                | Способ вызова                         | Описание                              |
| ---------------------- | ------------------------------------- | ------------------------------------- |
| `material-edit-save`   | `data-umami-event` — клик «Сохранить» | Сохранение названия, свойство: `type` |
| `material-edit-cancel` | `data-umami-event` — клик «Отменить»  | Отмена редактирования                 |

### Дублирование материала (features.materials.duplicate)

| Событие                               | Способ вызова                                   | Описание                                          |
| ------------------------------------- | ----------------------------------------------- | ------------------------------------------------- |
| `material-duplicate-select-classroom` | `data-umami-event` — выбор кабинета             | Выбор целевого кабинета, свойство: `classroom-id` |
| `material-duplicate-access-selector`  | `data-umami-event` — открытие селектора доступа | Выбор режима доступа при дублировании             |
| `material-duplicate-access-mode`      | `data-umami-event` — выбор режима               | Режим доступа, свойство: `mode`                   |
| `material-duplicate-confirm`          | `data-umami-event` — клик «Дублировать»         | Подтверждение дублирования                        |
| `material-duplicate-cancel`           | `data-umami-event` — клик «Отменить»            | Отмена дублирования                               |

### Создание заметки (features.materials.add)

| Событие                          | Способ вызова                                                      | Описание                                                                       |
| -------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------ |
| `material-create-note-draft`     | `data-umami-event` — клик «Создать заметку» (без выпадающего меню) | Быстрое создание заметки-черновика                                             |
| `material-create-note-menu-open` | `data-umami-event` — клик по триггеру выпадающего меню             | Открытие меню выбора режима доступа                                            |
| `material-create-note`           | `data-umami-event` — клик по пункту в выпадающем меню              | Создание заметки, свойство: `access-mode` (read_write / read_only / no_access) |

### Создание доски (features.materials.add)

| Событие                           | Способ вызова                                                    | Описание                                                                     |
| --------------------------------- | ---------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `material-create-board-draft`     | `data-umami-event` — клик «Создать доску» (без выпадающего меню) | Быстрое создание доски-черновика                                             |
| `material-create-board-menu-open` | `data-umami-event` — клик по триггеру выпадающего меню           | Открытие меню выбора режима доступа                                          |
| `material-create-board`           | `data-umami-event` — клик по пункту в выпадающем меню            | Создание доски, свойство: `access-mode` (read_write / read_only / no_access) |

---

## `/schedule` — Расписание

**Пакет:** `pages.calendar`, **Модуль:** `modules.calendar`

### Навигация по неделям (десктоп)

| Событие                | Способ вызова                                        | Описание                                |
| ---------------------- | ---------------------------------------------------- | --------------------------------------- |
| `schedule-prev-week`   | `data-umami-event` — стрелка «назад»                 | Переход к предыдущей неделе             |
| `schedule-next-week`   | `data-umami-event` — стрелка «вперёд»                | Переход к следующей неделе              |
| `schedule-week-picker` | `data-umami-event` — клик по диапазону дат           | Открытие календаря выбора недели        |
| `schedule-add-lesson`  | `data-umami-event` — клик «Добавить занятие» в шапке | Открытие модалки добавления (репетитор) |

### Канбан расписания (десктоп)

| Событие                            | Способ вызова                                    | Описание                                |
| ---------------------------------- | ------------------------------------------------ | --------------------------------------- |
| `schedule-add-lesson-column`       | `data-umami-event` — «+» в заголовке колонки дня | Добавление занятия на выбранный день    |
| `schedule-empty-add-lesson-kanban` | `data-umami-event` — кнопка в пустой колонке     | Добавление занятия из пустого состояния |
| `schedule-lesson-open`             | `data-umami-event` — клик по карточке занятия    | Открытие модалки «Информация о занятии» |

### Мобильный вид расписания

| Событие                      | Способ вызова                                  | Описание                                                                           |
| ---------------------------- | ---------------------------------------------- | ---------------------------------------------------------------------------------- |
| `schedule-add-lesson-mobile` | `data-umami-event` — кнопка «Добавить занятие» | Открытие модалки добавления                                                        |
| `schedule-date-select`       | `data-umami-event` — клик по дню               | Выбор даты; на мобильной недельной карусели доп. свойство `source` = `mobile-week` |

### Модалки занятий (features.lesson.\*)

Общие для `/schedule`, главной, кабинета и канбана.

#### Добавление (features.lesson.add)

| Событие                      | Способ вызова                                   | Описание                                                      |
| ---------------------------- | ----------------------------------------------- | ------------------------------------------------------------- |
| `lesson-add-submit`          | `data-umami-event` — клик «Добавить»            | Создание занятия                                              |
| `lesson-add-cancel`          | `data-umami-event` — клик «Отменить»            | Отмена создания                                               |
| `modal-schedule-go-to-today` | `data-umami-event` — «К сегодня» в левой панели | Прокрутка карусели к сегодня (в модалках добавления/переноса) |

#### Перенос (features.lesson.move)

| Событие              | Способ вызова                         | Описание               |
| -------------------- | ------------------------------------- | ---------------------- |
| `lesson-move-submit` | `data-umami-event` — клик «Перенести» | Подтверждение переноса |
| `lesson-move-cancel` | `data-umami-event` — клик «Отменить»  | Отмена переноса        |

#### Информация о занятии (features.lesson.info)

| Событие                   | Способ вызова                               | Описание                        |
| ------------------------- | ------------------------------------------- | ------------------------------- |
| `lesson-info-reschedule`  | `data-umami-event` — клик «Перенести»       | Открытие модалки переноса       |
| `lesson-info-edit`        | `data-umami-event` — иконка «Редактировать» | Открытие модалки редактирования |
| `lesson-info-cancel-init` | `data-umami-event` — иконка «Отменить»      | Открытие модалки отмены         |

#### Редактирование (features.lesson.change)

| Событие                | Способ вызова                         | Описание                       |
| ---------------------- | ------------------------------------- | ------------------------------ |
| `lesson-change-save`   | `data-umami-event` — клик «Сохранить» | Сохранение названия и описания |
| `lesson-change-cancel` | `data-umami-event` — клик «Отменить»  | Отмена редактирования          |

#### Отмена (features.lesson.cancel)

| Событие                   | Способ вызова                                         | Описание                        |
| ------------------------- | ----------------------------------------------------- | ------------------------------- |
| `lesson-cancel-this`      | `data-umami-event` — «Отменить это»                   | Отмена одного занятия серии     |
| `lesson-cancel-following` | `data-umami-event` — «Отменить это и все последующие» | Отмена серии с текущего занятия |
| `lesson-cancel-single`    | `data-umami-event` — «Отменить занятие»               | Отмена разового занятия         |
| `lesson-cancel-dismiss`   | `data-umami-event` — «Закрыть»                        | Закрытие модалки без отмены     |

---

## `/board/$boardId` — Доска

**Модуль:** `modules.board`

| Событие                   | Способ вызова                                                     | Описание                                                          |
| ------------------------- | ----------------------------------------------------------------- | ----------------------------------------------------------------- |
| `board-back`              | `data-umami-event` — клик по кнопке «Назад»                       | Возврат на предыдущую страницу                                    |
| `board-edit-name`         | `data-umami-event` — клик по имени доски                          | Открытие модалки редактирования названия                          |
| `board-timer-menu`        | `data-umami-event` — клик по иконке таймера                       | Открытие меню таймера                                             |
| `board-toggle-focus-mode` | `data-umami-event` — клик по кнопке фокус-режима                  | Переключение фокус-режима, свойство: `state` (enter / exit)       |
| `board-settings-menu`     | `data-umami-event` — клик по иконке настроек                      | Открытие выпадающего меню настроек                                |
| `board-toggle-lock`       | `data-umami-event` — клик «Заблокировать / Разблокировать» в меню | Переключение блокировки доски, свойство: `state` (pause / resume) |
| `board-download`          | `data-umami-event` — клик «Скачать» в меню                        | Скачивание доски                                                  |
| `board-clear`             | `data-umami-event` — клик «Очистить» в меню                       | Очистка содержимого доски                                         |
| `board-hotkeys-help`      | `data-umami-event` — пункт «Горячие клавиши»                      | Открытие справки по горячим клавишам                              |
| `board-input-mode`        | `data-umami-event` — выбор режима ввода                           | Смена режима ввода, свойство: `mode`                              |
| `board-import-json`       | `data-umami-event` — пункт «Импорт JSON»                          | Импорт доски из JSON                                              |
| `board-toggle-debug-info` | `data-umami-event` — переключатель отладки                        | Показ/скрытие отладочной информации                               |
| `board-lock-all`          | `data-umami-event` — «Заблокировать всё»                          | Блокировка всех слоёв                                             |
| `board-lock-category`     | `data-umami-event` — блокировка категории                         | Блокировка категории, свойство: `category`                        |
| `board-unlock-all`        | `data-umami-event` — «Разблокировать всё»                         | Разблокировка всех слоёв                                          |
| `board-unlock-category`   | `data-umami-event` — разблокировка категории                      | Разблокировка категории, свойство: `category`                     |

### Таймер

| Событие             | Способ вызова                              | Описание                |
| ------------------- | ------------------------------------------ | ----------------------- |
| `board-timer-play`  | `data-umami-event` — кнопка «Старт»        | Запуск таймера          |
| `board-timer-pause` | `data-umami-event` — кнопка «Пауза»        | Пауза таймера           |
| `board-timer-reset` | `data-umami-event` — кнопка «Сброс»        | Сброс таймера           |
| `board-timer-close` | `data-umami-event` — закрытие меню таймера | Закрытие панели таймера |

### Совместная работа (follow)

| Событие                  | Способ вызова                                     | Описание                                                               |
| ------------------------ | ------------------------------------------------- | ---------------------------------------------------------------------- |
| `board-broadcast-follow` | `data-umami-event` — трансляция / остановка       | Начало или остановка трансляции вида, свойство: `state` (start / stop) |
| `board-follow-user`      | `data-umami-event` — следование за участником     | Начало или остановка следования, свойство: `state` (start / stop)      |
| `board-follow-user-stop` | `data-umami-event` — баннер «Перестать следовать» | Остановка режима следования                                            |

---

## Страница ошибки

**Пакет:** `common.ui`

| Событие                | Способ вызова                            | Описание                                                                              |
| ---------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------- |
| `outbound-link-click`  | `data-umami-event` — ссылка поддержки    | Переход по внешней ссылке, свойства: `url`, `type` (support-telegram / support-email) |
| `error-page-home-link` | `data-umami-event` — ссылка «На главную» | Переход на главную страницу                                                           |

---

## `/call/$callId` — Видеозвонок (ВКС)

**Модуль:** `modules.calls` (события в пакете `@xipkg/calls-hooks`)

> События звонка реализованы во внешнем пакете и подключаются через Umami-скрипт на странице. Имена событий ниже — ожидаемый контракт; проверяйте актуальность в `@xipkg/calls-hooks`.

### Верхняя панель

| Событие                  | Способ вызова                                             | Описание                                                             |
| ------------------------ | --------------------------------------------------------- | -------------------------------------------------------------------- |
| `call-back-button`       | `data-umami-event` — клик по кнопке «Назад»               | Выход из звонка (возврат на предыдущую страницу)                     |
| `call-toggle-layout`     | `data-umami-event` — клик по переключателю раскладки      | Смена раскладки видео, свойство: `layout`                            |
| `call-copy-link`         | `data-umami-event` — клик по иконке копирования ссылки    | Копирование ссылки на звонок                                         |
| `call-toggle-fullscreen` | `data-umami-event` — клик по кнопке полноэкранного режима | Переключение полноэкранного режима, свойство: `state` (enter / exit) |
| `call-settings-button`   | `data-umami-event` — клик по иконке настроек              | Открытие настроек звонка                                             |

### Нижняя панель

| Событие                   | Способ вызова                                           | Описание                                                     |
| ------------------------- | ------------------------------------------------------- | ------------------------------------------------------------ |
| `call-toggle-microphone`  | `data-umami-event` — клик по кнопке микрофона           | Вкл/выкл микрофона, свойство: `state` (on / off)             |
| `call-toggle-camera`      | `data-umami-event` — клик по кнопке камеры              | Вкл/выкл камеры, свойство: `state` (on / off)                |
| `call-toggle-screenshare` | `data-umami-event` — клик по кнопке демонстрации экрана | Вкл/выкл демонстрации экрана, свойство: `state` (on / off)   |
| `call-whiteboard-button`  | `data-umami-event` — клик по кнопке доски               | Открытие доски во время звонка                               |
| `call-back-to-board`      | `data-umami-event` — клик «Вернуться к доске»           | Возврат к доске из режима видео                              |
| `call-raise-hand`         | `data-umami-event` — клик по кнопке «Поднять руку»      | Поднятие / опускание руки, свойство: `state` (raise / lower) |
| `call-disconnect`         | `data-umami-event` — клик по кнопке отключения          | Отключение от звонка                                         |
| `call-toggle-chat`        | `data-umami-event` — клик по кнопке чата                | Открытие / закрытие чата, свойство: `state` (open / close)   |

### Чат

| Событие               | Способ вызова                                        | Описание                                                    |
| --------------------- | ---------------------------------------------------- | ----------------------------------------------------------- |
| `outbound-link-click` | `data-umami-event` — клик по ссылке в сообщении чата | Переход по внешней ссылке, свойства: `url`, `source` (chat) |
