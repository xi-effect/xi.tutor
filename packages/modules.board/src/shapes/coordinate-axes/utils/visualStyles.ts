/** Визуальные константы — не зависят от size фигуры доски. */
export const COORDINATE_AXES_VISUAL = {
  axisStrokeWidth: 1.5,
  plotStrokeWidth: 2.5,
  gridStrokeWidth: 1,
  gridOpacity: 0.22,
  arrowSize: 6,
} as const;
