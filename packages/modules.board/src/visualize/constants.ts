/** Временно скрывает визуализацию на доске, код пайплайна остаётся. */
export const BOARD_VISUALIZE_ENABLED = false;

/** Ниже порога действие «Визуализировать» не показывается. */
export const VISUALIZE_MIN_CONFIDENCE = 0.55;

/** Один очевидный intent выше порога можно показать прямой кнопкой. */
export const VISUALIZE_DIRECT_ACTION_CONFIDENCE = 0.85;
