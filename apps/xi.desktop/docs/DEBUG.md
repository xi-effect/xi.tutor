# Отладка Electron приложения

## 🔧 Как открыть DevTools

### В production версии:

**Горячие клавиши:**

- **macOS**: `Cmd + Option + I`
- **Windows/Linux**: `Ctrl + Shift + I`

Или через консоль (если DevTools уже открыт):

```javascript
// В консоли DevTools
window.electronAPI?.toggleDevTools();
```

### В dev версии:

DevTools открываются автоматически при запуске.

## 🐛 Диагностика белого экрана

Если видите белый экран:

1. **Откройте DevTools** (см. выше)
2. **Проверьте консоль** на наличие ошибок
3. **Проверьте вкладку Network** - загружаются ли файлы
4. **Проверьте логи в терминале:**

```bash
# Запустите приложение из терминала для просмотра логов
/Applications/Sovlium.app/Contents/MacOS/Sovlium
```

Или через Console.app:

```bash
open -a Console
# Затем найдите логи приложения "Sovlium"
```

## 📋 Типичные проблемы

### 1. Файлы не найдены

**Симптомы:** Белый экран, ошибки в консоли о missing files

**Решение:**

- Убедитесь, что сборка прошла успешно: `pnpm build`
- Проверьте, что `xi.web/build` существует
- Пересоберите приложение

### 2. CORS ошибки

**Симптомы:** Ошибки в консоли о CORS policy

**Решение:**

- Проверьте настройки `webSecurity` в `src/main/index.ts`
- Убедитесь, что файлы загружаются через `file://` протокол

### 3. Путь к ресурсам неправильный

**Симптомы:** Ошибки загрузки index.html

**Решение:**

- Проверьте логи в консоли - там будет указан путь, который пытается использовать приложение
- Убедитесь, что файлы находятся в `Resources/xi.web/build/`

## 🔍 Проверка структуры приложения

Проверьте, что файлы на месте:

```bash
# Проверить структуру собранного приложения
ls -la /Applications/Sovlium.app/Contents/Resources/xi.web/build/

# Должен быть файл index.html
```

## 📝 Логирование

Приложение логирует важную информацию в консоль:

- Путь к веб-приложению
- Путь к ресурсам
- Ошибки загрузки

Все логи видны в терминале при запуске из командной строки или в Console.app.
