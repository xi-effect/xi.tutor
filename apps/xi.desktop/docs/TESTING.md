# Инструкция по установке и тестированию

## 📦 Установка на macOS

### Вариант 1: Через DMG файл (рекомендуется)

1. **Откройте DMG файл:**

   ```bash
   open release/Sovlium-1.0.0.dmg
   ```

   Или просто дважды кликните на файл в Finder.

2. **Установите приложение:**
   - В открывшемся окне перетащите приложение `Sovlium.app` в папку `Applications`
   - Или скопируйте через терминал:
     ```bash
     cp -R release/mac/Sovlium.app /Applications/
     ```

3. **Запустите приложение:**
   ```bash
   open /Applications/Sovlium.app
   ```
   Или найдите его в Launchpad/Applications и запустите.

### Вариант 2: Через ZIP архив

1. **Распакуйте архив:**

   ```bash
   cd release
   unzip Sovlium-1.0.0-mac.zip
   ```

2. **Переместите приложение:**

   ```bash
   mv Sovlium.app /Applications/
   ```

3. **Запустите:**
   ```bash
   open /Applications/Sovlium.app
   ```

### ⚠️ Разрешение на запуск (первый запуск)

При первом запуске macOS может показать предупреждение о безопасности:

1. **Если видите сообщение "Sovlium не может быть открыт":**
   - Перейдите в **Системные настройки** → **Конфиденциальность и безопасность**
   - Найдите сообщение о блокировке приложения
   - Нажмите **"Открыть в любом случае"** или **"Разрешить"**

2. **Альтернативный способ (через терминал):**

   ```bash
   # Удалить карантин (если приложение заблокировано)
   xattr -cr /Applications/Sovlium.app

   # Затем запустить
   open /Applications/Sovlium.app
   ```

## 🧪 Тестирование приложения

### Базовое тестирование

1. **Проверка запуска:**
   - Приложение должно открыться без ошибок
   - Окно должно появиться с плавной анимацией
   - Title bar должен быть виден (на macOS)

2. **Проверка функциональности:**
   - Войдите в систему (если требуется авторизация)
   - Проверьте навигацию по приложению
   - Убедитесь, что все страницы загружаются корректно
   - Проверьте работу основных функций

3. **Проверка интеграции с Electron:**
   ```javascript
   // В консоли DevTools (Cmd+Option+I)
   if (window.electronAPI) {
     console.log('Electron API доступен');
     window.electronAPI.getVersion().then(console.log);
   }
   ```

### Тестирование на разных архитектурах

Для тестирования на разных архитектурах:

```bash
# Сборка только для текущей архитектуры
pnpm run build:electron -- --mac --x64    # Intel
pnpm run build:electron -- --mac --arm64  # Apple Silicon

# Или обе архитектуры (универсальный бинарник)
pnpm run build:electron
```

### Проверка логов

Если приложение не запускается, проверьте логи:

```bash
# Логи системы
log show --predicate 'process == "Sovlium"' --last 5m

# Или через Console.app
open -a Console
```

### Тестирование обновлений

Для тестирования обновлений приложения:

1. Установите первую версию
2. Измените версию в `package.json`
3. Соберите новую версию
4. Проверьте, что обновление работает корректно

## 🪟 Установка на Windows

1. **Через установщик NSIS:**
   - Запустите `Sovlium Setup 1.0.0.exe`
   - Следуйте инструкциям установщика
   - Приложение будет установлено в Program Files

2. **Portable версия:**
   - Распакуйте `Sovlium-1.0.0.exe` в любую папку
   - Запустите `Sovlium.exe` напрямую

## 🐧 Установка на Linux

### AppImage (портативная версия)

```bash
# Сделать исполняемым
chmod +x Sovlium-1.0.0.AppImage

# Запустить
./Sovlium-1.0.0.AppImage
```

### DEB пакет (Debian/Ubuntu)

```bash
# Установка
sudo dpkg -i sovlium_1.0.0_amd64.deb

# Если есть зависимости
sudo apt-get install -f

# Запуск
sovlium
```

## 🔍 Отладка собранного приложения

### Включение DevTools в production

Если нужно отладить production версию, можно временно добавить в `src/main/index.ts`:

```typescript
// В production тоже открывать DevTools (только для отладки!)
if (!isDev) {
  win.webContents.openDevTools();
}
```

### Проверка путей к ресурсам

Убедитесь, что веб-приложение загружается корректно:

```javascript
// В консоли DevTools
console.log(window.location.href);
// Должно быть file:// путь к index.html
```

## 📝 Чек-лист тестирования

- [ ] Приложение запускается без ошибок
- [ ] Title bar отображается корректно (macOS)
- [ ] Иконка приложения правильная
- [ ] Все страницы загружаются
- [ ] Навигация работает
- [ ] Авторизация работает
- [ ] Основные функции работают
- [ ] Нет ошибок в консоли
- [ ] Приложение корректно закрывается
- [ ] Нет утечек памяти при длительной работе

## 🚀 Быстрый тест после сборки

```bash
# macOS - быстрая установка и запуск
cd apps/xi.desktop
open release/Sovlium-1.0.0.dmg
# Перетащите в Applications, затем:
open /Applications/Sovlium.app
```

## 🐛 Устранение проблем

### Приложение не запускается

1. Проверьте логи (см. выше)
2. Убедитесь, что все зависимости собраны
3. Проверьте права доступа:
   ```bash
   chmod +x /Applications/Sovlium.app/Contents/MacOS/Sovlium
   ```

### Ошибки загрузки веб-контента

1. Проверьте, что `xi.web/build` существует
2. Убедитесь, что файлы скопированы в `resources/xi.web/build/`
3. Проверьте пути в `src/main/index.ts`

### Проблемы с иконками

1. Убедитесь, что файлы скопированы: `pnpm run build:icons`
2. Проверьте размеры иконок (минимум 512x512 для PNG)
3. Пересоберите приложение
