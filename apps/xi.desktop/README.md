# Sovlium Desktop

Electron-обёртка для веб-приложения `xi.web`.

## 🚀 Быстрый старт

### Разработка

```bash
pnpm dev
```

Запускает:

- Dev server `xi.web` на `http://localhost:5173`
- Electron приложение с **автоматическим HMR**

### Настройка портов

По умолчанию:

- **xi.web**: порт `5173`
- **xi.desktop**: подключается к порту `5173`

Если нужно изменить порт для `xi.web`, можно:

1. Изменить порт в `apps/xi.web/vite.config.ts` (параметр `server.port`)
2. Или запустить с другим портом: `cd apps/xi.web && pnpm dev --port 5174`
3. В `xi.desktop` можно переопределить порт через переменную окружения:
   ```bash
   WEB_APP_PORT=5174 pnpm dev
   ```

### Сборка

```bash
pnpm build
```

Создаёт установочные файлы в папке `release/`.

## 📦 Архитектура

- **Main процесс**: `src/main/` - Electron main процесс
- **Preload**: `src/main/preload.ts` - Безопасный мост между main и renderer
- **Веб-приложение**: Используется `xi.web` (в dev - через dev server, в production - собранная версия)

## 🔧 Особенности

- ✅ Полная поддержка HMR через WebSocket от Vite
- ✅ Безопасная архитектура (contextIsolation, без nodeIntegration)
- ✅ Автоматическая перезагрузка при сбоях dev server
- ✅ Готовые IPC методы для управления окном
- ✅ Настроенные иконки для всех платформ
- ✅ Hardened Runtime для macOS
- ✅ Настроенные установщики (DMG, NSIS, AppImage, DEB)

## 📝 Использование Electron API в xi.web

Если нужно использовать Electron API в веб-приложении, доступен `window.electronAPI`:

```typescript
// В xi.web
if (window.electronAPI) {
  const version = await window.electronAPI.getVersion();
  await window.electronAPI.minimize();
}
```

Типы доступны в `src/renderer/src/electron.d.ts` (можно скопировать в `xi.web` при необходимости).

## 🎨 Иконки и ресурсы

Иконки приложения находятся в `build/icons/`:

- Автоматически ссылаются на файлы из `xi.web/public/`
- Поддерживаются все платформы (macOS, Windows, Linux)
- См. [build/README.md](./build/README.md) для подробностей

## 📚 Подробная документация

- [BUILD.md](./BUILD.md) - детальные инструкции по сборке и установке
- [TESTING.md](./TESTING.md) - инструкции по установке и тестированию приложения
